'use client';
import { CreateServerModel } from '@/components/Models';
import React from 'react';

const ModelProvider = () => {
	return (
		<>
			<CreateServerModel />
		</>
	);
};

export default ModelProvider;
